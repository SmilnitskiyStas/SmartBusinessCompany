@model IEnumerable<ProjectType>
@{
    ViewData["Title"] = "Types Page";
}

<h1>Type Index Page</h1>

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="text-white">Type List</h1>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="ProjectType" asp-action="ProjectTypeCreate" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Create new type</a>
            </div>
        </div>
        <div class="row">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            ID
                        </th>
                        <th>
                            Name
                        </th>
                        <th>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.ProjectTypeId
                            </td>
                            <td>
                                @item.Name
                            </td>

                            <td class="text-end">
                                <a asp-action="ProjectTypeUpdate" asp-route-projectTypeId="@item.ProjectTypeId" class="btn btn-success">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                                <a asp-action="ProjectTypeDelete" asp-route-projectTypeId="@item.ProjectTypeId" class="btn btn-danger">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>